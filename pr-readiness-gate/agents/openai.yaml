interface:
  display_name: "PR Readiness Gate"
  short_description: "Check whether a branch is truly ready for PR prep."
  default_prompt: "Run the PR readiness gate on the current branch before preparing or updating the PR."
