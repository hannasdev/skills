interface:
  display_name: "Milestone Implementation"
  short_description: "Build and commit one initiative milestone from a packet."
  default_prompt: "Implement the selected milestone from the implementation packet, commit the finished changes, and report what changed."
