interface:
  display_name: "Post-Merge Cleanup"
  short_description: "Clean local state after PR merge."
  default_prompt: "Run post-merge cleanup for the merged PR and report the final local state."
